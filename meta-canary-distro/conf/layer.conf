# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-canary-distro"
BBFILE_PATTERN_meta-canary-distro = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-canary-distro = "6"

LAYERDEPENDS_meta-canary-distro = "core"
LAYERSERIES_COMPAT_meta-canary-distro = "scarthgap"
