# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-canary-project"
BBFILE_PATTERN_meta-canary-project = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-canary-project = "6"

LAYERDEPENDS_meta-canary-project = "core"
LAYERSERIES_COMPAT_meta-canary-project = "scarthgap"
